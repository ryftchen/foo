include_guard()

if(NOT TARGET_PROCESSOR STREQUAL "")
    add_compile_options(-D_TARGET_PROCESSOR="${TARGET_PROCESSOR}")
endif()
if(PRECOMPILED_HEADER)
    add_compile_options(-D_PRECOMPILED_HEADER)
endif()
if(RUNTIME_PRINTING)
    add_compile_options(-D_RUNTIME_PRINTING)
endif()
